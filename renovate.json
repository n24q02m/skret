{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "labels": ["dependencies"],
  "schedule": ["before 5am every weekday"],
  "prConcurrentLimit": 3,
  "semanticCommits": "enabled",
  "commitMessagePrefix": "fix(deps):",
  "commitMessageAction": "update",
  "commitMessageTopic": "{{depName}}",
  "semanticCommitType": "fix",
  "semanticCommitScope": "deps",
  "golang": {
    "postUpdateOptions": ["gomodTidy"]
  },
  "packageRules": [
    {
      "matchPackageNames": ["astro"],
      "allowedVersions": "<6.4.0",
      "description": "Hold astro <6.4 until @astrojs/starlight + @astrojs/mdx support it. astro 6.4.0 removed the astro/jsx/rehype.js export that @astrojs/mdx 5.0.6 (transitive via starlight) imports, breaking the Deploy Docs CD build."
    },
    {
      "matchPackageNames": ["pnpm"],
      "allowedVersions": "<11",
      "description": "Hold pnpm <11. pnpm 11 makes ERR_PNPM_IGNORED_BUILDS (esbuild/sharp) fatal, breaking 'pnpm install --frozen-lockfile' in the docs Deploy Docs CD job."
    }
  ]
}
