# Binaries
/skret
*.exe
*.dll
*.so
*.dylib
dist/

# Test
coverage
coverage.html
coverage.*
*.out
*.test

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets (local dev provider files)
.secrets.*.yaml
.secrets.*.yml
.env
.env.*
.dev.vars

# Build
vendor/

# Node (Astro Starlight docs)
node_modules/
docs/dist/
docs/.astro/

# Branding asset raw outputs (regenerable via scripts/gen-branding.py)
docs/public/raw/

# Installer scripts are sourced from repo root; CD syncs to docs/public/ at build time
docs/public/install.sh
docs/public/install.ps1

# Python bytecode
__pycache__/
*.pyc

# Agent artifacts (not committed)
# .jules/ is deliberately absent: the bot ledgers under it are versioned, and
# the review bots read them from the repo. Ignoring it made `git add` refuse.
.code-review-graph/
.claude-plugin/
.wrangler/

# Vault-dashboard hub: rendered deploy config with real account/KV IDs (never commit)
hub/wrangler.deploy.jsonc

# AI Assistant / Rules
.superpower/
superpower/
superpowers/
docs/superpowers/

