--- local.go
+++ local.go
@@ -118,13 +118,13 @@
	}

	if _, err := tmp.Write(data); err != nil {
-		tmp.Close()
-		os.Remove(tmpPath)
+		_ = tmp.Close()
+		_ = os.Remove(tmpPath)
		return fmt.Errorf("local: write tmp file: %w", err)
	}

	if err := tmp.Close(); err != nil {
-		os.Remove(tmpPath)
+		_ = os.Remove(tmpPath)
		return fmt.Errorf("local: close tmp file: %w", err)
	}

@@ -127,7 +127,7 @@
	}

	if err := os.Rename(tmpPath, p.path); err != nil {
-		os.Remove(tmpPath)
+		_ = os.Remove(tmpPath)
		return fmt.Errorf("local: rename file: %w", err)
	}
